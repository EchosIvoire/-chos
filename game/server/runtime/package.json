{
  "name": "terres-fauves-runtime",
  "version": "0.0.1",
  "private": true,
  "description": "Logique serveur autoritative Nakama (TypeScript) pour Terres Fauves.",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --target=es2017 --format=iife --outfile=../nakama/data/modules/index.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "nakama-runtime": "^1.31.0",
    "typescript": "^5.4.5"
  }
}
